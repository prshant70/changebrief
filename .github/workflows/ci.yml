name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  tests:
    name: tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests (coverage >= 70%)
        run: |
          # Coverage is measured only for the changebrief package (not tests/tools at repo root).
          pytest -q \
            --cov=changebrief \
            --cov-report=term-missing \
            --cov-fail-under=70

